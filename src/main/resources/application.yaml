spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/rentauto
    username: postgres
    password: murad
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
